# Series 16: Per-Head Replication + Blending Combo + Out_Proj Slicing

**Files:** `cum/tensor/per_head_muon.py`, `colab_benchmark_v10.ipynb`
**Status:** PENDING

## Context
Series 15b discovered per-head orthogonalization: -0.025 vs Muon (1 run). Bigger than the entire blending recipe (-0.018). Now we need to:
1. Replicate the per-head result
2. Combine per-head with blending recipe (TD(λ) + temporal + d=-1.0)
3. Try per-head on out_proj (column-wise slicing)
4. Wire it all together

## Tests

### 16a: Replication of Per-Head 4 Slices
Run PerHead 4 slices 3x to confirm -0.025 is real.

| Run | Val Loss | vs Muon | Notes |
|-----|----------|---------|-------|
| 15b (original) | 1.4991 | -0.0253 | |
| 16a run 1 | | | |
| 16a run 2 | | | |
| 16a run 3 | | | |

### 16b: Per-Head + Blending Recipe
Combine PerHead 4 slices with CUM12v2 TD(λ=0.5) + temporal + d=-1.0.
If effects are orthogonal/additive, expect ~-0.034 vs Muon.

| Config | Val Loss | vs Muon | Time |
|--------|----------|---------|------|
| Muon+AdamW (baseline) | | | |
| PerHead 4 slices (plain) | | | |
| PerHead 4 + combined blend | | | |
| PerHead 4 + TD(λ) + temporal + d=-1.0 | | | |

### 16c: Out_Proj Column Slicing + Full Wire-Up
out_proj is (d_model, d_model) = (128, 128). Conceptually it maps FROM head-concatenated space.
Columns 0-31 correspond to head 0's output, columns 32-63 to head 1, etc.
To slice by head: transpose → split rows → NS each (128, 32) slice → recombine → transpose back.

| Config | Val Loss | vs Muon | Time |
|--------|----------|---------|------|
| Muon+AdamW (baseline) | | | |
| PerHead QKV only (4 slices) | | | |
| PerHead QKV + out_proj (both 4 slices) | | | |
| Full wire: QKV + out_proj per-head + blending | | | |

## Analysis
[To be filled after testing]

## Verdict
[To be filled after testing]
